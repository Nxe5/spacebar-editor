node_modules/
package-lock.json
yarn.lock
bun.lockb
dist/
sidecar/node_modules/
src-tauri/target/
.env
.env.*
!.env.example
*.log
.DS_Store

# Spacebar Editor per-workspace runtime (when this repo is opened as a project)
.sidebar/state.json
.sidebar/.lock
.sidebar/runs/

tests/llm/results/*
!tests/llm/results/.gitkeep
tests/llm/fixtures/workspace/

# Separate Homebrew tap repo, cloned locally for publishing casks
homebrew-spacebar/

# Tauri updater private signing key — lives in GitHub Actions secrets
# (TAURI_SIGNING_PRIVATE_KEY), never committed. Public key's contents are
# baked into tauri.conf.json instead of kept as a loose file.
src-tauri/updater.key
